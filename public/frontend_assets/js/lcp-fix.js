document.addEventListener('DOMContentLoaded', () => {
  const lcpElement = document.querySelector('.slider-title');
  if (lcpElement) {
    lcpElement.style.visibility = 'visible';
    lcpElement.style.opacity = '1';
  }
});